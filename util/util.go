package util

import (
    "time"
)

const ApplicationName    = `qcat`
const ApplicationVersion = `0.1.0`
const ApplicationSummary = `utility for publishing and consuming data from an AMQP message broker`

var StartedAt = time.Now()
